name: nid_poule_app
description: Nids de poule tracker
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.5.0 <4.0.0"  # ✅ Upgradé pour Dart 3.11

dependencies:
  flutter:
    sdk: flutter

  # ✅ Versions compatibles Flutter 3.41
  flutter_map: ^7.0.2
  latlong2: ^0.9.1

  firebase_core: ^3.7.0
  cloud_firestore: ^5.4.4
  firebase_storage: ^12.3.3

  file_picker: ^8.1.2  # ✅ Fix warnings

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true